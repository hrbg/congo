script(type='text/javascript')
  now.prependPaste = function(paste){
    $('#pastes').prepend("<li><span><a href='/pastes/" + paste.code +"'>view</a></span><code class='highlight'>"+ paste.preview +"</code></li>");
  };
ul#pastes
  != partial("paste", pastes)